# Public repository data boundary

This engineering repository intentionally contains no production records, crawl cache, article text, video metadata, image objects, human-review worksheets, or historical extraction output.

The empty manifests below exist only so imports and local smoke tests have a deterministic starting point. Use synthetic fixtures or the separately published sanitized demo repository when exercising the UI.
